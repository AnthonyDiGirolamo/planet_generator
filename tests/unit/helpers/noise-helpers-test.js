import {
  noiseHelpers
} from '../../../helpers/noise-helpers';
import { module, test } from 'qunit';

module('NoiseHelpersHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = noiseHelpers(42);
  assert.ok(result);
});
