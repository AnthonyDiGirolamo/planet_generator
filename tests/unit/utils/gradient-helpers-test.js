import gradientHelpers from '../../../utils/gradient-helpers';
import { module, test } from 'qunit';

module('gradientHelpers');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = gradientHelpers();
  assert.ok(result);
});
